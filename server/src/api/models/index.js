const {
  create, getByAuthor, getAll, getById, remove, update, register,
} = require('./task/task');

module.exports = {
  create,
  getByAuthor,
  getAll,
  getById,
  remove,
  update,
  register,
};
